<configuration>
    <!-- Define properties with default values -->
    <property name="LOG_DIR" value="${log.dir:-logs}"/>
    <property name="LOG_FILE" value="${log.file:-application.log}"/>
    <property name="MAX_HISTORY" value="${log.history:-30}"/>
    <property name="MAX_SIZE" value="${log.size:-10MB}"/>
    <property name="LOG_LEVEL" value="${log.level:-INFO}"/>
    <property name="LOG_CONSOLE_ENABLED" value="${log.console.enabled:-true}"/>

    <!-- Rolling File Appender -->
    <appender name="ROLLING" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${LOG_FILE}</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/%d{yyyy-MM-dd}-${LOG_FILE}</fileNamePattern>
            <maxHistory>${MAX_HISTORY}</maxHistory>
        </rollingPolicy>

        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <maxFileSize>${MAX_SIZE}</maxFileSize>
        </triggeringPolicy>

        <encoder>
            <pattern>[ %d{yyyy-MM-dd HH:mm:ss} ] [ %thread ] [ %-5level ] [ %logger{36} ] ~ %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Root Logger -->
    <root level="${LOG_LEVEL}">
        <appender-ref ref="ROLLING"/>
    </root>

    <!-- Conditionally Enable Console Logging -->
    <springProfile name="console">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>[ %d{yyyy-MM-dd HH:mm:ss} ] [ %thread ] [ %-5level ] [ %logger{36} ] ~ %msg%n</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <logger name="ROOT" level="${LOG_LEVEL}">
            <appender-ref ref="CONSOLE"/>
        </logger>
    </springProfile>

</configuration>
